ECS (Fargate) module placeholder.

Intended responsibilities:
- ECS cluster
- Task definitions per service
- ECS services per service
- IAM roles (task/execution)
- CloudWatch Logs groups

Each microservice remains an independent deployment unit.
